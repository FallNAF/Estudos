package fundamentos.classesEMetodos;
	/* Será visto sobre as classes como a abaixo.
	 * a classe publica inicial, é obrigatório ser o mesmo nome do arquivo .java, 
	 * tendo maiuscula e minuscula.
	 * 
	 * Até o ponto atual, é feito basicamente usando a classe publica padrão e
	 * criando o método main
	 * 
	 * Toda classe envolve um bloco {} de código, possuindo ou não o metódo main
	 */
public class Classe {

	public static void main(String[] args) {
		
	}
}
